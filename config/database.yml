# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
development:
  adapter: postgresql
  database: deep_thought_development
  host: localhost
  username: Izhou

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  database: deep_thought_test
  host: localhost
  username: Izhou

production:
  adapter: postgresql
  database: deep_thought_production
  host: localhost
  username: Izhou
